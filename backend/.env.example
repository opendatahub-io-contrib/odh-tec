AWS_S3_ENDPOINT=https://my.endpoint.com
AWS_ACCESS_KEY_ID=XXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=XXXXXXXXXXXXXXXX
AWS_DEFAULT_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name
HF_TOKEN=hf_XXXXXXXXXXXXX
MAX_CONCURRENT_TRANSFERS=2
HTTP_PROXY=http://proxy-server:3128
HTTPS_PROXY=http://proxy-server:3128

# ===================================
# Local Storage Configuration
# ===================================

# Comma-separated list of allowed local storage paths
# These paths must exist and be accessible to the application
# Default: /opt/app-root/src/data
LOCAL_STORAGE_PATHS=/opt/app-root/src/data,/opt/app-root/src/models

# Maximum file size for uploads and transfers (in GB)
# Files larger than this limit will be rejected
# Default: 20
MAX_FILE_SIZE_GB=20

# ===================================
# CORS Configuration
# ===================================

# Comma-separated list of allowed origins for production
# Development default: http://localhost:8888,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:8888,http://localhost:3000

# ===================================
# Authentication Configuration
# ===================================

# JWT secret for token signing and verification
# IMPORTANT: Use a strong random string in production (e.g., openssl rand -base64 32)
# This secret is used to sign and verify JWT tokens for API authentication
JWT_SECRET=your-secret-key-change-in-production

# ===================================
  # File Type Validation
  # ===================================

  # Override default allowed file extensions (comma-separated, with or without dots)
  # When set, ONLY these extensions are allowed (replaces defaults entirely)
  # Default includes: model files (.safetensors, .pt, .h5), data files (.csv, .json),
  # documents (.pdf, .docx, .xlsx), images (.jpg, .png), archives (.zip, .tar), etc.
  # Example: ALLOWED_FILE_EXTENSIONS=.pdf,.docx,.xlsx,.custom
  # ALLOWED_FILE_EXTENSIONS=

  # Append additional allowed file extensions to the defaults (comma-separated)
  # When set, these extensions are added to the default allowed list
  # Use this to allow custom file types while keeping all defaults
  # Example: ALLOWED_FILE_EXTENSIONS_APPEND=.custom,.proprietary
  # ALLOWED_FILE_EXTENSIONS_APPEND=

  # Override default blocked file extensions (comma-separated, with or without dots)
  # When set, ONLY these extensions are blocked (replaces defaults entirely)
  # WARNING: Use with caution - default blocked list includes dangerous executable types
  # (.exe, .dll, .sh, .bat, .js, .py, .php, etc.) for security
  # Example: BLOCKED_FILE_EXTENSIONS=.dangerous,.restricted
  # BLOCKED_FILE_EXTENSIONS=

  # Append additional blocked file extensions to the defaults (comma-separated)
  # When set, these extensions are added to the default blocked list
  # Use this to block additional custom file types
  # Example: BLOCKED_FILE_EXTENSIONS_APPEND=.risky,.unsafe
  # BLOCKED_FILE_EXTENSIONS_APPEND=


